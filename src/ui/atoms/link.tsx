import styled from 'styled-components';

export const NavLink = styled.a`
  color: #999;
  text-decoration: none;
  font-size: inherit;
  transition: 0.35s;
  font-weight: 800;
  &:hover {
    color: #111;
  }
`;
